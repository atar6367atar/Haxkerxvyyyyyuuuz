services:
  - type: worker
    name: telegram-python-runner
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
    plan: free  # ⚡ Render free tier'da SINIRSIZ çalışır!
